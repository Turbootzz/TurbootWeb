# Database
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/turboot_db?schema=public"

# GitHub API Token (optional, recommended)
# Generate a personal access token at: https://github.com/settings/tokens
# Only needs public_repo scope for public repositories
# Increases rate limit from 60 to 5000 requests per hour
GITHUB_TOKEN=""

# Email Configuration (optional)
EMAIL_HOST=""
EMAIL_PORT=""
EMAIL_USER=""
EMAIL_PASS=""
EMAIL_FROM="info@turboot.com"

# Analytics (optional)
NEXT_PUBLIC_GA_ID=""